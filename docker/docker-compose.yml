services:
  arangodb:
    image: arangodb/arangodb:3.12
    ports:
      - "8529:8529"
    environment:
      - ARANGO_ROOT_PASSWORD=rootpassword
    volumes:
      - arangodata:/var/lib/arangodb3
    networks:
      - aletheia-network

networks:
  aletheia-network:
    driver: bridge

volumes:
  arangodata:
